import React from 'react';
import Styled from 'styled-components';

const StyledUserWidget = Styled.div`
    background: #ded;
`

const UserWidget = ({current_track}) => {
    return (
        <StyledUserWidget className="user-widget">

        </StyledUserWidget>
    )
}

export default UserWidget;

